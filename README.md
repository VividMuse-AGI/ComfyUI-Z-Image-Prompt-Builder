# ComfyUI-Z-Image-Prompt-Builder

由 VividMuse 开发的 ComfyUI 中文写真提示词节点。通过写真预设、结构化下拉字段和可复现的随机组合，生成适用于 Z-Image 与 Z-Image-Turbo 的中文自然语言正向提示词。

## 功能

- 只输出中文正向提示词，不生成负面提示词。
- 遵循 Z-Image 的自然语言描述方式，不使用传统权重标签和“8K、杰作”等质量元标签。
- 支持精简、标准、详细三种提示词密度，默认使用标准模式。
- 92 个结构化字段分属画面基础、人物、发型、服装、姿态动作、场景、摄影和视觉表现八个模块；每项可选择“跟随预设”“随机抽取”“不使用”或指定内容，依赖字段会按当前模式折叠显示。
- 人物模块将年龄、族裔、脸型、轮廓、眼型、瞳色、眼睑、肤色、肤质、妆容和身形拆为原子字段；妆容支持整体预设与分项自定义两条互斥分支。
- 成像媒介独立于写真题材，内置 12 种数码、手机、胶片与即时成像媒介。
- 写真主题采用“12 个写真大类＋120 个具体主题”的两级筛选结构。
- 主题级随机会联动背景与光线，覆盖日常、商业、自然、运动、中式、电影与幻想等场景方向。
- 发型拆分为发色、长度、卷度、造型、刘海和头部配饰六个基础字段，并提供可选的发色色调与染色方式；随机生成优先使用 37 套兼容发型结构。
- 服装拆分为穿搭结构加 20 个子字段，覆盖连衣裙、连体服、上装、下装、颜色、材质、图案、版型、袜装、鞋履与服装配件；不同穿搭结构互斥显示，颜色和材质分别归属具体衣物。
- 服装选项覆盖 14 种连衣裙、8 种连体服、14 种上装、12 种下装、16 种颜色、14 种材质与 12 种图案；随机组合从 17 套人像穿搭方向中筛选兼容素材。
- 姿态动作拆分为画面瞬间、基础姿态、身体方向、身体重心、肩颈、手部、腿部、头部、视线和表情十个维度；随机时从 21 套完整动作链中整体抽取。
- 场景拆分为场景大类、具体地点、时间、天气、前景、背景、环境细节、空间材质和空间层次；12 个空间大类可筛选 86 个不重复地点，随机时从 61 套完整场景组合中整体抽取，室内场景自动省略天气。
- 摄影拆分为景别、画面布局、等效焦段、拍摄距离、机位、景深和对焦位置七个字段，分别提供 11、13、10、9、11、8、9 个选项；随机时从 22 套完整摄影方案中整体抽取。
- 视觉表现拆分为主光来源、方向、质地、照明落点、阴影、主配色、色温、对比、影像风格、细节质地、高光和颗粒十二个字段；随机时分别从 21 套完整布光方案和 21 套视觉风格中抽取。
- 随机结果由种子控制；相同预设、字段状态、随机范围与种子会得到相同提示词。随机时优先保留手动锁定值，再应用主题、场景、媒介和模块兼容规则。
- 摄影方案会按横竖画幅、写真大类和坐姿／站姿筛选，避免焦段、距离、景别和对焦目标互相冲突。
- 输出推荐宽度和推荐高度，便于设置实际 latent 尺寸。
- 前端提供“🎲 生成随机组合”按钮。
- 前端以“当前编辑模块”在画面基础、人物、发型、服装、姿态动作、场景、摄影和视觉表现之间切换；一次只显示所选模块，切换模块只改变显示，不修改 92 个结构化字段的任何值。
- 工作流保存后会恢复上次选择的当前编辑模块；没有界面状态的旧工作流默认打开“画面基础”。“仅启用当前模块”是明确改变模块启用状态的操作；“清空结构化模块”保留自由提示词，“全部清空”清除两部分。
- 无第三方 Python 依赖，不加载模型，不占用显存。

## 内置写真预设

- 日系森系夏日柔光写真
- 日系咖啡馆暖调近景人像
- 夜间室内轻奢硬闪时尚写真
- 都市职场轻奢坐姿写真
- 自定义组合

四套预设来自实际使用过的写真提示词，并拆分为人物、造型、动作、环境、光线、构图和成像等可编辑字段。

## 安装

### Git 安装

在 ComfyUI 的 `custom_nodes` 目录中执行：

```bash
git clone https://github.com/VividMuse/ComfyUI-Z-Image-Prompt-Builder.git
```

重新启动 ComfyUI，并对浏览器执行一次强制刷新。节点位于：

```text
VividMuse → Z-Image → Z-Image 中文提示词生成器
```

### 手动安装

将整个项目目录复制到：

```text
ComfyUI/custom_nodes/ComfyUI-Z-Image-Prompt-Builder
```

然后重新启动 ComfyUI。项目没有额外依赖，因此不需要执行 `pip install`。

## 使用方法

1. 添加“Z-Image 中文提示词生成器”节点。
2. 选择写真预设和提示词密度。
3. 保留“跟随预设”，或在任意字段中指定内容。
4. 需要变化的字段选择“随机抽取”，设置随机范围和随机种子。
5. 也可以点击节点底部的“🎲 生成随机组合”。
6. 在“当前编辑模块”中选择要编辑的分类；界面一次只显示该模块，切换模块只改变显示并保留每个字段值。需要改变启用状态时，点击“仅启用当前模块”；按需使用“清空结构化模块”保留手写内容，或使用“全部清空”清除两部分。
7. 将“中文提示词”连接到文本编码节点。
8. 将“推荐宽度”和“推荐高度”连接到兼容的 latent 宽高输入，或手动填写相同数值。

### 自由提示词与紧凑单模块编辑器

1. 在“自由提示词”中输入自己编写的主体内容。
2. 从“当前编辑模块”选择画面基础、人物、发型、服装、姿态动作、场景、摄影或视觉表现；一次只显示选中的模块。
3. 切换模块只改变显示，保留全部 92 个结构化字段的值；保存并重新加载工作流后，会恢复上次编辑的模块。没有界面状态的旧工作流默认打开“画面基础”。
4. 点击“仅启用当前模块”才会改变模块启用状态，再调整该分类中的下拉字段。
5. 使用“拼接位置”决定自由文本在前还是结构化模块在前。
6. “清空结构化模块”保留手写内容；“全部清空”同时清除两部分。

切换预设和生成随机组合都不会修改自由提示词。

字段值为“跟随预设”时，后端读取当前预设；字段值为“随机抽取”时，后端按预设兼容池和随机种子选取；字段值为“不使用”时，该模块不会进入提示词。手动指定的字段始终锁定，不会被后端随机覆盖。全部结构化模块均为“不使用”且自由提示词为空时，中文提示词输出空字符串；自由提示词非空时，仍输出该文本，推荐宽高仍按当前预设提供。

“自定义组合”不是空白预设，它自带一套最少可用的基线值（2:3 竖构图、全画幅微单、20–29 岁东亚等）。想从空白开始，请手动把字段设为“不使用”，或点击“清空结构化模块”按钮。

“写真大类”是界面过滤字段，不直接写入提示词；它用于把“写真主题”限制在同一题材中。例如选择“旅行度假”后，具体主题只显示海边、酒店、山野、古镇、泳池与公路旅行方向。选择“随机抽取”时，后端会先确定大类和具体主题，再按主题约束成像媒介、背景、光线、姿态、构图与质感。横竖画幅和坐姿/站姿会进一步筛选镜头组合；用户手动指定的值仍保持最高优先级。

## 提示词密度

- **精简**：保留主体、核心外观、主要动作、场景锚点与关键摄影信息，以直接可见的画面信息为主。
- **标准**：保留常用的服装、动作、场景、主光和镜头意图，省略摄影距离、精确人物占比等重复控制。
- **详细**：保留全部字段细节，并展开精确拍摄距离与完整摄影说明，适合复杂服装、全身造型或需要精确复现的画面。

实际字符数会随启用模块、字段选择和自由提示词变化；密度控制的是信息层级，不是 Z-Image 的官方字数限制。

## 随机范围

- **局部微调（动作、表情、视觉表现）**：保持主要人物、服装、场景和构图，只改变动作链及色彩、对比、质地、高光和颗粒等视觉细节。
- **同主题重拍（保留主题和人物）**：保留画面比例、写真主题、年龄阶段和族裔身份，变化其余字段。
- **跨风格混搭（全部字段）**：允许所有字段进入全局素材池，获得跨度更大的组合。

v0.4 使用上述三个随机范围；旧的 76 字段工作流不在本次干净升级的兼容范围内。

随机服装时，袜装、鞋履与图案由抽中的穿搭配方决定：配方未包含对应配件时该字段输出“不使用”（例如多数配方不配袜装）；版型细节与服装配件则始终从全量选项池随机。发型结构、姿态动作、场景、布光、视觉与摄影分别从兼容组合中整套抽取。

## 输出

| 输出 | 类型 | 用途 |
| --- | --- | --- |
| 中文提示词 | `STRING` | 连接 Z-Image 文本编码节点 |
| 推荐宽度 | `INT` | 设置 latent 宽度 |
| 推荐高度 | `INT` | 设置 latent 高度 |

推荐尺寸：

| 画面比例 | 宽度 | 高度 |
| --- | ---: | ---: |
| 2:3 竖构图 | 832 | 1248 |
| 3:4 竖构图 | 768 | 1024 |
| 4:5 竖构图 | 896 | 1120 |
| 9:16 竖构图 | 720 | 1280 |
| 1:1 方形构图 | 1024 | 1024 |
| 3:2 横构图 | 1248 | 832 |
| 4:3 横构图 | 1024 | 768 |
| 5:4 横构图 | 1120 | 896 |
| 16:9 横构图 | 1280 | 720 |

提示词中的比例用于帮助模型理解构图方向；最终画幅仍由 latent 的实际宽度和高度决定。

横构图随机时不会只交换画布宽高：系统会优先从十套保留横向空间的摄影方案中选择，包括环境人像、带环境全身、左右三分线、视线方向留白、对称构图与动态广角。用户手动锁定的景别、布局、焦段、距离、机位、景深或对焦位置不受这一偏好覆盖。

## 提示词结构

节点按以下顺序组织一段自然语言：

```text
画面方向、成像媒介与写真主题 → 年龄阶段、族裔大类、地域族裔分支与关键五官 → 发型、身形与服装
→ 一个姿态和动作链 → 地点、时间、前后景与两到三个环境细节
→ 主光来源、方向、质地、落点与阴影 → 配色、色温、对比、高光与颗粒
→ 景别、画面布局、等效焦段、拍摄距离、机位、景深与对焦位置
```

标准模式会自动合并同义控制，将十二项视觉表现压缩为一段自然语言，并在摄影部分省略精确拍摄距离；详细模式才展开完整布光、成像与七项摄影控制。视觉随机会先服从用户锁定，再按场景、写真大类和成像媒介筛选，避免手机摄影、胶片与商业数码质感互相冲突。

年龄采用六个十年阶段，实际提示词使用阶段中点作为视觉锚点，例如“20–29岁”输出“25岁左右”。族裔采用两级菜单：九个族裔大类负责筛选，地域族裔分支提供斯拉夫裔、北欧裔、阿拉伯裔等可选细化；选择具体分支时，分支会替代大类进入提示词。默认输出示例为“一位25岁左右的东亚成年女性”。年龄与族裔均不会自动绑定肤色、五官、发色、发型、服装或文化场景。

## 兼容性

- 开发测试目标：ComfyUI 0.31.1
- 已知测试环境：秋叶整合包、PyTorch 2.9.1+cu130
- 节点只处理字符串和整数，原则上不受 CUDA、显卡或模型权重版本影响

## 开发测试

项目要求 Python 3.10+，节点运行时无第三方依赖。发布前可运行以下静态检查，验证语法、数据结构与组句逻辑：

```bash
python -m py_compile nodes.py __init__.py
node --check web/js/preset_sync.js
python -c "import nodes; nodes.ZImageChinesePromptBuilder().build_prompt()"
```

## 发布信息

- 当前版本：`0.1.0`
- GitHub：[VividMuse/ComfyUI-Z-Image-Prompt-Builder](https://github.com/VividMuse/ComfyUI-Z-Image-Prompt-Builder)
- Comfy Registry Publisher ID：`VividMuse`
- 目标宿主：ComfyUI `0.31.1` 及以上
- 许可证：MIT

Registry 发布包通过 `.comfyignore` 排除测试、浏览器原型和内部计划，但保留正式节点、前端脚本以及全部运行时 JSON 词库。

## 许可证

[MIT License](LICENSE)
