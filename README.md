# ComfyUI-Z-Image-Prompt-Builder

> [English](README.en.md) | 中文

由 VividMuse 开发的 ComfyUI 中文写真提示词节点。通过写真预设、结构化下拉字段和可复现的随机组合，生成适用于 Z-Image 与 Z-Image-Turbo 的中文自然语言正向提示词。

## 功能

- 只输出中文正向提示词，不生成负面提示词。
- 遵循 Z-Image 的自然语言描述方式，不使用传统权重标签和“8K、杰作”等质量元标签。
- 支持精简、标准、详细三种提示词密度，默认使用标准模式。
- 92 个结构化字段分属画面基础、人物、发型、服装、姿态动作、场景、摄影和视觉表现八个模块；每项可选择“跟随预设”“随机抽取”“不使用”或指定内容，依赖字段会按当前模式折叠显示。
- 人物模块将年龄、族裔、脸型、轮廓、眼型、瞳色、眼睑、肤色、肤质、妆容和身形拆为原子字段；妆容支持整体预设与分项自定义两条互斥分支。
- 成像媒介独立于写真题材，内置 12 种数码、手机、胶片与即时成像媒介。
- 写真主题采用“12 个写真大类＋144 个具体主题”的两级筛选结构。
- 主题级随机会联动背景与光线，覆盖日常、商业、自然、运动、中式、电影与幻想等场景方向。
- 发型拆分为发色、长度、卷度、造型、刘海和头部配饰六个基础字段，并提供可选的发色色调与染色方式；随机生成优先使用 37 套兼容发型结构。
- 服装拆分为穿搭结构加 20 个子字段，覆盖连衣裙、连体服、上装、下装、颜色、材质、图案、版型、袜装、鞋履与服装配件；不同穿搭结构互斥显示，颜色和材质分别归属具体衣物。
- 服装选项覆盖 25 种连衣裙、12 种连体服、30 种上装、20 种下装、22 种颜色、20 种材质与 17 种图案；随机组合从 19 套人像穿搭方向中筛选兼容素材。
- 姿态动作拆分为画面瞬间、基础姿态、身体方向、身体重心、肩颈、手部、腿部、头部、视线和表情十个维度；随机时从 32 套完整动作链中整体抽取。
- 场景拆分为场景大类、具体地点、时间、天气、前景、背景、环境细节、空间材质和空间层次；12 个空间大类可筛选 102 个不重复地点，随机时从 71 套完整场景组合中整体抽取，室内场景自动省略天气。
- 摄影拆分为景别、画面布局、等效焦段、拍摄距离、机位、景深和对焦位置七个字段；随机时从 30 套完整摄影方案中整体抽取。
- 视觉表现拆分为主光来源、方向、质地、照明落点、阴影、主配色、色温、对比、影像风格、细节质地、高光和颗粒十二个字段；随机时分别从 29 套完整布光方案和 30 套视觉风格中抽取。
- 随机结果由种子控制；相同预设、字段状态、随机范围与种子会得到相同提示词。随机时优先保留手动锁定值，再应用主题、场景、媒介和模块兼容规则。
- 摄影方案会按横竖画幅、写真大类和坐姿／站姿筛选，避免焦段、距离、景别和对焦目标互相冲突。
- 输出推荐宽度和推荐高度，便于设置实际 latent 尺寸。
- 前端提供“🎲 生成随机组合”按钮。
- 前端以“当前编辑模块”在画面基础、人物、发型、服装、姿态动作、场景、摄影和视觉表现之间切换；一次只显示所选模块，切换模块只改变显示，不修改 92 个结构化字段的任何值。
- 除完整生成器外，额外提供 8 个可串联的独立模块节点；每个节点只显示本模块字段，并通过“前置提示词 → 组合提示词”逐段拼接，不需要的节点可用 `Ctrl+B` 旁路。
- 提供独立的“TXT提示词库”和“TXT模块词库”节点，可在模块链中复用个人完整提示词或结构化模块短语，无需把词库控件复制到每个模块节点。
- 工作流保存后会恢复上次选择的当前编辑模块；没有界面状态的旧工作流默认打开“画面基础”。“仅启用当前模块”是明确改变模块启用状态的操作；“清空结构化模块”保留自由提示词，“全部清空”清除两部分。
- TXT 用户词库支持导入完整中文提示词，并按“添加到后面”“添加到前面”或“替换自由提示词”写入自由提示词框。
- TXT 结构化模块词库支持替换八个标准模块，也支持“自定义”类型；自定义条目启用后作为独立片段追加在标准模块之后。
- 两类 TXT 词库均保存在节点工作流中；支持最多 500 条、最大 1MB 的本地 `.txt` 文件，不会加载模型或占用显存。
- 无第三方 Python 依赖，不加载模型，不占用显存。

## 内置写真预设

- 日系草地单车夏日柔光写真
- 日系咖啡馆暖调近景人像
- 夜间室内轻奢硬闪时尚写真
- 都市职场轻奢坐姿写真
- 古风汉服园林柔光写真
- 海边夏日泳装写真
- 赛博都市夜景写真
- 影棚水光妆美容特写
- 落地窗瑜伽塑形写真
- 旅馆窗边电影静帧
- 自定义组合

十套写真预设被拆分为人物、造型、动作、环境、光线、构图和成像等可编辑字段；“自定义组合”提供最少可用的中性起点。

## 安装

### Git 安装

在 ComfyUI 的 `custom_nodes` 目录中执行：

```bash
git clone https://github.com/VividMuse-AGI/ComfyUI-Z-Image-Prompt-Builder.git
```

重新启动 ComfyUI，并对浏览器执行一次强制刷新。节点位于：

```text
VividMuse → Z-Image → Z-Image 中文提示词生成器
VividMuse → Z-Image → 模块 → 8 个独立模块节点
VividMuse → Z-Image → 词库 → 2 个独立 TXT 节点
```

### 手动安装

将整个项目目录复制到：

```text
ComfyUI/custom_nodes/ComfyUI-Z-Image-Prompt-Builder
```

然后重新启动 ComfyUI。项目没有额外依赖，因此不需要执行 `pip install`。

推荐从 [`v0.3.0` Release](https://github.com/VividMuse-AGI/ComfyUI-Z-Image-Prompt-Builder/releases/tag/v0.3.0) 下载附件 `ComfyUI-Z-Image-Prompt-Builder-v0.3.0.zip`，直接解压到 `custom_nodes`；安装包内部已经固定为 `ComfyUI-Z-Image-Prompt-Builder`，无需改名。

GitHub 自动生成的 `Source code (zip)` 和 `Source code (tar.gz)` 会在目录名后附加版本号，这是正常行为，但不作为推荐安装入口。如果使用自动源码包，请将解压目录改名为 `ComfyUI-Z-Image-Prompt-Builder`，并确认 `custom_nodes` 中没有其他版本副本。

### 更新已有安装

Git 安装可在节点目录中执行：

```bash
git pull
```

手动安装请先备份自己的 TXT 词库，然后用新版本项目文件替换旧版本。更新完成后：

1. 重新启动 ComfyUI。
2. 在浏览器中按 `Ctrl+F5` 强制刷新，避免旧前端脚本缓存。
3. 删除画布上的旧节点并新建一个节点，确认新控件与紧凑布局已加载；已有工作流可在确认后继续使用。

不要同时保留两个同名节点目录，否则 ComfyUI 可能重复加载或加载到旧版本。

## 使用方法

1. 添加“Z-Image 中文提示词生成器”节点。
2. 选择写真预设和提示词密度。
3. 保留“跟随预设”，或在任意字段中指定内容。
4. 需要变化的字段选择“随机抽取”，设置随机范围和随机种子。
5. 也可以点击“🎲 生成随机组合”。
6. 在“当前编辑模块”中选择要编辑的分类；界面一次只显示该模块，切换模块只改变显示并保留每个字段值。需要改变启用状态时，点击“仅启用当前模块”；按需使用“清空结构化模块”保留手写内容，或使用“全部清空”清除两部分。
7. 将“中文提示词”连接到文本编码节点。
8. 将“推荐宽度”和“推荐高度”连接到兼容的 latent 宽高输入，或手动填写相同数值。

### 独立模块节点

适合希望在画布上明确组织提示词结构、并随时旁路某一部分的工作流。现有完整节点继续保留，两种方式可以按需选择。

推荐连接顺序：

```text
画面基础 → 人物 → 发型 → 服装 → 姿态动作 → 场景 → 摄影 → 视觉表现
```

- 每个节点接收可选的“前置提示词”，输出“组合提示词”；第一段可以不连接输入。
- 画面基础节点额外输出推荐宽度和推荐高度。
- 每个模块有独立的预设、提示词密度和随机种子，并提供“生成本模块随机组合”“全部跟随模块预设”“清空本模块”三个快捷按钮。
- 将某个模块节点设为 `Ctrl+B` 旁路后，ComfyUI 会把输入直接传给后续节点，适合临时移除该段。
- 各节点不共享可编辑的下拉框状态；但直接串联时，“组合提示词”会在运行期携带上游已经解析的字段，使下游随机摄影能够参考实际姿态、场景等兼容信息。TXT 提示词库会原样保留这份信息；TXT 模块词库替换某个标准模块时会清除该模块的旧结构化字段，避免下游继续按照已经被替换的姿态或场景筛选。由于任意 TXT 短语无法可靠反解析成全部下拉字段，被替换模块只按“用户自定义内容”处理。
- 如果中途接入会重新创建普通字符串的第三方文本节点，上述运行期信息可能丢失；如需由一套预设统一管理全部 92 个字段，继续使用“Z-Image 中文提示词生成器”。
- “TXT提示词库”适合插入完整自由提示词；“TXT模块词库”适合按画面基础、人物、发型、服装、姿态动作、场景、摄影、视觉表现或自定义类型插入一个模块片段。

### 自由提示词与紧凑单模块编辑器

1. 在“自由提示词”中输入自己编写的主体内容。
2. 从“当前编辑模块”选择画面基础、人物、发型、服装、姿态动作、场景、摄影或视觉表现；一次只显示选中的模块。
3. 切换模块只改变显示，保留全部 92 个结构化字段的值；保存并重新加载工作流后，会恢复上次编辑的模块。没有界面状态的旧工作流默认打开“画面基础”。
4. 点击“仅启用当前模块”才会改变模块启用状态，再调整该分类中的下拉字段。
5. 使用“拼接位置”决定自由文本在前还是结构化模块在前。
6. “清空结构化模块”保留手写内容；“全部清空”同时清除两部分。

切换预设和生成随机组合都不会修改自由提示词。

字段值为“跟随预设”时，后端读取当前预设；字段值为“随机抽取”时，后端按预设兼容池和随机种子选取；字段值为“不使用”时，该模块不会进入提示词。手动指定的字段始终锁定，不会被后端随机覆盖。全部结构化模块均为“不使用”且自由提示词为空时，中文提示词输出空字符串；自由提示词非空时，仍输出该文本，推荐宽高仍按当前预设提供。

“自定义组合”不是空白预设，它自带一套最少可用的基线值（2:3 竖构图、全画幅微单、20–29 岁东亚等）。想从空白开始，请手动把字段设为“不使用”，或点击“清空结构化模块”按钮。

“写真大类”是界面过滤字段，不直接写入提示词；它用于把“写真主题”限制在同一题材中。例如选择“旅行度假”后，具体主题只显示海边、酒店、山野、古镇、泳池与公路旅行方向。选择“随机抽取”时，后端会先确定大类和具体主题，再按主题约束成像媒介、背景、光线、姿态、构图与质感。横竖画幅和坐姿/站姿会进一步筛选镜头组合；用户手动指定的值仍保持最高优先级。

## TXT 词库

节点提供两套用途不同的本地 TXT 词库：

- **TXT 用户词库**：保存完整提示词，选中后写入“自由提示词”。
- **TXT 模块词库**：保存某一个结构化模块的短语，应用后替换对应标准模块，或启用一个额外的“自定义”片段。

两个面板默认折叠。节点中的排列顺序为：结构化操作按钮 → TXT 模块词库 → TXT 用户词库 → 自由提示词。点击面板标题可展开或收起；收起只隐藏控件，不会清除已经导入或应用的内容。

### TXT 用户词库

适合保存一整段可以独立使用的提示词，例如完整的人像、商品图或场景描述。

1. 展开“📚 TXT用户词库”。
2. 点击“导入TXT词库”，选择本地 `.txt` 文件。
3. 从“词库条目”选择标题。
4. 在“词库加入方式”中选择“添加到后面”“添加到前面”或“替换自由提示词”。
5. 点击“添加到自由提示词”。

推荐使用带标题的分块格式：

```text
## 日系咖啡馆近景
标签：人像，咖啡馆，暖调
3:4竖构图，真实写实暖调咖啡馆近景人像，一位25岁左右的东亚成年女性坐在深棕色皮质卡座上……
---

## 城市雨夜人像
标签：人像，夜景，电影感
3:2横构图，真实写实城市雨夜环境人像……
```

格式规则：

- `## 标题`：必需的条目名称；重复标题会自动附加序号。
- `标签：`：可省略，使用中文或英文逗号分隔。标签不会进入最终提示词；当前版本暂不提供标签筛选，它主要用于文件内整理和后续扩展。
- 正文：标题和标签之后的所有内容，可以是一行或多行。
- `---`：建议用于分隔条目；遇到下一个 `## 标题` 时也会自动结束上一条。
- 说明性注释应放在第一个 `## 标题` 之前，并以 `#` 开头。

如果整个文件完全没有 `## 标题`，节点会启用简易格式：每个非空、非注释行都被识别为一条独立提示词。简易格式不适合跨行提示词。

三种加入方式只影响“自由提示词”：

| 加入方式 | 结果 |
| --- | --- |
| 添加到后面 | 保留原自由提示词，把所选条目拼接到末尾 |
| 添加到前面 | 把所选条目放在原自由提示词之前 |
| 替换自由提示词 | 用所选条目覆盖自由提示词 |

节点会在两段文字之间自动补充必要的中文分号；已有句号、逗号、分号或换行时不会重复添加。

完整示例见 [`examples/TXT词库示例.txt`](examples/TXT词库示例.txt)。

### TXT 结构化模块词库

适合保存可复用的人物、发型、服装、动作、场景、摄影或视觉短语。它不是完整提示词词库，每个条目只应描述一个模块。

1. 展开“🧩 TXT模块词库（全模块）”。
2. 点击“导入结构化模块TXT词库”。
3. 在“词库模块”中选择模块。
4. 从“模块词库条目”选择同模块下的一条内容。
5. 点击“应用到……模块”；选择“自定义”时，按钮会显示“启用自定义模块”。

模块词库必须使用分块格式，并且每条都需要 `模块：`：

```text
## 清透日系女性
模块：人物
标签：东亚，20岁左右，清透
一位25岁左右的东亚成年女性，小巧鹅蛋脸，深棕色杏仁眼，暖白自然肤质，清透裸粉妆。
---

## 商品版式留白
模块：自定义
标签：商品，版式，留白
人物右侧保留大面积干净留白，前景加入一只透明香水瓶作为视觉锚点。
```

可使用的标准模块名称为：

| 模块 | 建议内容 |
| --- | --- |
| 画面基础 | 画面比例、成像媒介、写真主题 |
| 人物 | 年龄、族裔、五官、肤色、妆容、身形 |
| 发型 | 发色、长度、卷度、造型、刘海、头饰 |
| 服装 | 穿搭结构、颜色、材质、图案、鞋袜和配饰 |
| 姿态动作 | 姿态、重心、肢体、视线和表情 |
| 场景 | 地点、时间、天气、前后景和环境细节 |
| 摄影 | 景别、构图、焦段、距离、机位、景深和焦点 |
| 视觉表现 | 光线、阴影、色彩、对比、质地、高光和颗粒 |

也可识别“基础画面、人物设定、角色、头发、穿搭、姿态、动作、环境、镜头、视觉、光影色彩”等别名。导入后界面会统一显示为标准名称。

### 标准模块与自定义模块

- 应用到八个标准模块之一时，用户模块正文会作为该模块的完整替代文本；只要它非空，同模块的内置下拉字段就不会重复输出。
- “自定义”不会替换八个标准模块。它作为独立结构化片段追加在八个模块之后，适合商品道具、版式留白、文字区域、叙事条件等额外要求。
- “拼接位置”控制自由提示词位于整段结构化内容之前还是之后；自定义模块仍属于结构化内容。
- 切换预设或生成随机组合不会删除已经应用的用户模块。点击“仅启用当前模块”会清除其他模块的用户替代文本，避免隐藏模块继续输出。

完整示例见 [`examples/TXT模块词库示例.txt`](examples/TXT模块词库示例.txt)。

### 清空与清除按钮

| 按钮 | 会清除 | 会保留 |
| --- | --- | --- |
| 清空自由提示词 | 自由提示词文本 | 已导入的 TXT 用户词库、结构化字段、用户模块 |
| 清除已导入词库 | TXT 用户词库的条目列表 | 已经写入自由提示词的文本 |
| 清空当前用户模块 | 当前“词库模块”对应的已应用文本 | 模块词库条目、其他用户模块、内置字段 |
| 清除模块词库 | TXT 模块词库的条目列表 | 已经应用到各模块的文本 |
| 清空结构化模块 | 92 个内置结构化字段及全部已应用用户模块 | 自由提示词、两类已导入词库 |
| 全部清空 | 自由提示词、结构化字段及全部已应用用户模块 | 两类已导入词库，方便重新选择 |
| 仅启用当前模块 | 其他标准模块字段及其他用户模块文本 | 当前标准模块、自由提示词、两类已导入词库 |

“清空”通常针对已经写入节点、会参与输出的内容；“清除词库”只卸载可选择的条目列表。这样可以在移除源词库后继续保留已经应用的提示词。

### 文件限制与工作流保存

| 限制 | 数值 |
| --- | ---: |
| 文件格式 | `.txt` 纯文本 |
| 单个文件大小 | 最大 1MB |
| 单个词库条目数 | 最多 500 条 |
| 单条提示词正文 | 最多 20000 个字符 |

这些限制用于避免超大文本拖慢 ComfyUI 前端、节点序列化和工作流保存。词库在浏览器本地读取，不会由本节点主动上传到网络；导入内容会随节点属性保存在工作流中，因此把工作流分享给他人时，也可能同时分享其中的词库文本。

建议使用 UTF-8 编码。提示词应使用简洁、肯定、可直接拼接的中文自然语言；没有的内容直接省略，不写“不佩戴头饰”等反面描述。

## 提示词密度

- **精简**：保留主体、核心外观、主要动作、场景锚点与关键摄影信息，以直接可见的画面信息为主。
- **标准**：保留常用的服装、动作、场景、主光和镜头意图，省略摄影距离、精确人物占比等重复控制。
- **详细**：保留全部字段细节，并展开精确拍摄距离与完整摄影说明，适合复杂服装、全身造型或需要精确复现的画面。

实际字符数会随启用模块、字段选择和自由提示词变化；密度控制的是信息层级，不是 Z-Image 的官方字数限制。

## 随机范围

- **局部微调（动作、表情、视觉表现）**：保持主要人物、服装、场景和构图，只改变动作链及色彩、对比、质地、高光和颗粒等视觉细节。
- **同主题重拍（保留主题和人物）**：保留画面比例、写真主题、年龄阶段和族裔身份，变化其余字段。
- **跨风格混搭（全部字段）**：允许所有字段进入全局素材池，获得跨度更大的组合。

当前公开版使用上述三个随机范围。旧的 76 字段开发期工作流不在正式版的兼容范围内。

随机服装时，袜装、鞋履与图案由抽中的穿搭配方决定：配方未包含对应配件时该字段输出“不使用”（例如多数配方不配袜装）；版型细节与服装配件则始终从全量选项池随机。发型结构、姿态动作、场景、布光、视觉与摄影分别从兼容组合中整套抽取。

## 输出

| 输出 | 类型 | 用途 |
| --- | --- | --- |
| 中文提示词 | `STRING` | 连接 Z-Image 文本编码节点 |
| 推荐宽度 | `INT` | 设置 latent 宽度 |
| 推荐高度 | `INT` | 设置 latent 高度 |

推荐尺寸：

| 画面比例 | 宽度 | 高度 |
| --- | ---: | ---: |
| 2:3 竖构图 | 832 | 1248 |
| 3:4 竖构图 | 768 | 1024 |
| 4:5 竖构图 | 896 | 1120 |
| 9:16 竖构图 | 720 | 1280 |
| 9:21 竖构图 | 576 | 1344 |
| 1:1 方形构图 | 1024 | 1024 |
| 3:2 横构图 | 1248 | 832 |
| 4:3 横构图 | 1024 | 768 |
| 5:4 横构图 | 1120 | 896 |
| 16:9 横构图 | 1280 | 720 |
| 21:9 横构图 | 1344 | 576 |

提示词中的比例用于帮助模型理解构图方向；最终画幅仍由 latent 的实际宽度和高度决定。

横构图随机时不会只交换画布宽高：系统会优先从十套保留横向空间的摄影方案中选择，包括环境人像、带环境全身、左右三分线、视线方向留白、对称构图与动态广角。用户手动锁定的景别、布局、焦段、距离、机位、景深或对焦位置不受这一偏好覆盖。

## 提示词结构

节点按以下顺序组织一段自然语言：

```text
画面方向、成像媒介与写真主题 → 年龄阶段、族裔大类、地域族裔分支与关键五官 → 发型、身形与服装
→ 一个姿态和动作链 → 地点、时间、前后景与两到三个环境细节
→ 主光来源、方向、质地、落点与阴影 → 配色、色温、对比、高光与颗粒
→ 景别、画面布局、等效焦段、拍摄距离、机位、景深与对焦位置
```

标准模式会自动合并同义控制，将十二项视觉表现压缩为一段自然语言，并在摄影部分省略精确拍摄距离；详细模式才展开完整布光、成像与七项摄影控制。视觉随机会先服从用户锁定，再按场景、写真大类和成像媒介筛选，避免手机摄影、胶片与商业数码质感互相冲突。

年龄采用六个十年阶段，实际提示词使用阶段中点作为视觉锚点，例如“20–29岁”输出“25岁左右”。族裔采用两级菜单：九个族裔大类负责筛选，地域族裔分支提供斯拉夫裔、北欧裔、阿拉伯裔等可选细化；选择具体分支时，分支会替代大类进入提示词。默认输出示例为“一位25岁左右的东亚成年女性”。年龄与族裔均不会自动绑定肤色、五官、发色、发型、服装或文化场景。

## 兼容性

- 开发测试目标：ComfyUI 0.31.1
- 已知测试环境：秋叶整合包、PyTorch 2.9.1+cu130
- 同时适配 ComfyUI 经典节点界面与 Node 2.0；Node 2.0 下隐藏字段不会继续占用节点高度。
- 更新节点后必须重新启动 ComfyUI 并强制刷新浏览器，才能加载新的前端扩展脚本。
- 节点只处理字符串和整数，原则上不受 CUDA、显卡或模型权重版本影响

## 开发测试

项目要求 Python 3.10+，节点运行时无第三方依赖。发布前可运行以下静态检查，验证语法、数据结构与组句逻辑：

```bash
python -m py_compile nodes.py __init__.py
node --check web/js/preset_sync.js
node --check web/js/txt_library.js
node --check web/js/txt_module_library.js
python -m unittest discover -s tests -p "test_*.py" -v
python -c "import json,pathlib; [json.loads(p.read_text(encoding='utf-8')) for p in pathlib.Path('phrase_library').glob('*.json')]"
```

完整前端回归测试：

```bash
for test in tests/frontend_*.mjs; do node "$test"; done
```

Windows PowerShell 用户可以逐个运行 `tests/frontend_*.mjs`。GitHub Actions 会在推送和 Pull Request 时自动执行仓库中配置的测试。

最小运行检查：

```bash
python -c "import nodes; nodes.ZImageChinesePromptBuilder().build_prompt()"
```

## 发布信息

- 当前版本：`0.3.0`
- GitHub：[VividMuse-AGI/ComfyUI-Z-Image-Prompt-Builder](https://github.com/VividMuse-AGI/ComfyUI-Z-Image-Prompt-Builder)
- Comfy Registry Publisher ID：`VividMuse-AGI`
- 目标宿主：ComfyUI `0.31.1` 及以上
- 许可证：MIT

Registry 发布包通过 `.comfyignore` 排除测试、浏览器原型和内部计划，但保留正式节点、前端脚本以及全部运行时 JSON 词库。

## 许可证

[MIT License](LICENSE)
