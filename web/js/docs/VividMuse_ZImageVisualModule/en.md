# Z-Image Visual Style

Builds key-light source, direction, quality, placement, shadows, palette, color temperature, contrast, image style, detail rendering, highlights, and grain.

Use **None** to omit a visual instruction instead of writing a negative phrase. Randomization uses coordinated lighting and visual-style profiles.

Chain the Chinese and English inputs/outputs separately. Use `Ctrl+B` to bypass the module.
