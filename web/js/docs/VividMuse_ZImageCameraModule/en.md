# Z-Image Photography

Builds shot size, composition, equivalent focal length, camera distance, angle, depth of field, and focus point.

Random camera bundles account for portrait/landscape orientation and seated/standing poses. Manually selected values take priority over random compatibility choices.

Chain the Chinese and English inputs/outputs separately. Use `Ctrl+B` to bypass the module.
